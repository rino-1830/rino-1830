name: segment-boundary-qwen-base
on:
  push:
    branches: ['experiment/boundary-kv-bonsai']
    paths:
      - '.github/workflows/segment-boundary-qwen-base.yml'
  workflow_dispatch:
jobs:
  qwen-base:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      HF_HOME: /tmp/hf
      HF_HUB_DISABLE_TELEMETRY: '1'
      TOKENIZERS_PARALLELISM: 'false'
      OMP_NUM_THREADS: '4'
      MKL_NUM_THREADS: '4'
      MODEL_ID: Qwen/Qwen3-1.7B
      RESULT_PATH: boundary-compression-lab/results/segment_boundary_qwen_base.json
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: boundary-compression-lab/requirements-bonsai.txt
      - run: python -m pip install --index-url https://download.pytorch.org/whl/cpu torch
      - run: python -m pip install -r boundary-compression-lab/requirements-bonsai.txt
      - run: python boundary-compression-lab/experiment_segment_boundary.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: segment-boundary-qwen-base-results
          path: boundary-compression-lab/results/
          if-no-files-found: warn
